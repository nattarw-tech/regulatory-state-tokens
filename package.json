{
  "name": "regulatory-state-tokens",
  "version": "1.0.0",
  "description": "Regulation as Ledger State - credential-gated pre-settlement payment admission on the XRP Ledger",
  "scripts": {
    "test": "node --require ts-node/register --test test/*.test.ts",
    "preflight": "ts-node src/preflight.ts",
    "scenario:update": "ts-node src/scenarios/03-rule-update.ts",
    "scenario:revoke": "ts-node src/scenarios/04-revocation.ts",
    "demo": "ts-node src/demo.ts",
    "verify": "ts-node src/verify-evidence.ts"
  },
  "dependencies": {
    "xrpl": "^4.0.0",
    "dotenv": "^16.4.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "ts-node": "^10.9.2",
    "@types/node": "^20.0.0"
  }
}
